using System;

namespace BetterDecorator
{
    class Program
    {
        static void Main(string[] args)
        {
            string str = args[0];
            char chr = char.Parse(argrs[1]);
            int intg = int.Parse(args[2]);
            
        }
        private static string Decor(string s, char c, int i)
        {

        }
    }
}
